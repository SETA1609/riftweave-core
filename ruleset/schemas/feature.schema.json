{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Feature",
  "description": "A perk. In this classless ruleset, perks are the single character-defining progression table, gated by skill thresholds, prerequisite perks, and attributes. The `type` distinguishes how a perk is acquired: normal perks are chosen on level-up, `creation` perks (the former 'traits') are chosen at character creation and usually carry a tradeoff, and `racial_trait` perks are granted automatically by ancestry.",
  "type": "object",
  "properties": {
    "$schema": { "type": "string" },
    "features": {
      "type": "array",
      "items": { "$ref": "#/definitions/feature" }
    }
  },
  "required": ["features"],
  "additionalProperties": false,
  "definitions": {
    "feature": {
      "type": "object",
      "properties": {
        "id": { "$ref": "schema.json#/definitions/entryId" },
        "key": { "type": "string", "pattern": "^[a-z_][a-z0-9_]*$", "description": "Symbolic snake_case key (the old string id) for human readability when editing JSON." },
        "label": { "type": "string", "description": "Human-readable display name with spaces and capitalization." },
        "description": { "type": "string" },
        "source": { "$ref": "schema.json#/definitions/sourceRef" },
        "type": {
          "type": "string",
          "enum": ["perk", "creation", "racial_trait", "universal"],
          "description": "How the perk is acquired. perk = chosen from the perk table on level-up; creation = chosen at character creation (the former 'traits', usually with a tradeoff); racial_trait = granted automatically by ancestry (pinned to a race); universal = always available"
        },
        "category": {
          "type": "string",
          "enum": ["combat", "magic", "stealth", "social", "knowledge", "survival", "utility", "general"],
          "description": "Perk-tree grouping, mirroring skill categories"
        },
        "prerequisite": {
          "type": "object",
          "properties": {
            "level": { "type": "integer", "minimum": 1 },
            "abilities": {
              "type": "object",
              "description": "Minimum attribute values, keyed by attribute id (e.g. { \"agi\": 6 })",
              "additionalProperties": { "type": "integer", "minimum": 1, "maximum": 10 }
            },
            "skills": {
              "type": "object",
              "description": "Minimum skill values, keyed by skill id (e.g. { \"red_magic\": 50 })",
              "additionalProperties": { "type": "integer", "minimum": 1, "maximum": 100 }
            },
            "perks": {
              "type": "array",
              "description": "Prerequisite perk ids (numeric entryId) that must already be owned",
              "items": { "$ref": "schema.json#/definitions/entryId" }
            },
            "other": { "type": "string" }
          },
          "additionalProperties": false
        },
        "ranks": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum number of times this perk can be taken (1 = single-rank)"
        },
        "effects": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "type": { "type": "string" },
              "description": { "type": "string" },
              "target": {
                "description": "When the bonus/penalty is delivered via a registered effect (e.g. fortify_skill, resist, phobia), this is the numeric effect id. For ad-hoc bonus targets (hp_max, melee_damage, persuasion, etc.) it is a string key.",
                "oneOf": [
                  { "$ref": "schema.json#/definitions/entryId" },
                  { "type": "string" }
                ]
              },
              "value": {},
              "parameter": {
                "oneOf": [
                  { "$ref": "schema.json#/definitions/entryId" },
                  { "type": "string" }
                ],
                "description": "For generic parameterized effects (e.g. 'resist', 'restore_resource', 'fortify_attribute', 'phobia', 'allergy', 'cure'), specifies the subject. Use numeric entryId when referencing another collection's id (skill, material, etc.); string for vocabulary values."
              },
              "durationType": {
                "type": "string",
                "enum": ["temporal", "unlimited"],
                "description": "For fortify effects on skills/attributes/resources: 'unlimited' are stackable (additive magnitudes for permanent traits). 'temporal' override with the highest magnitude."
              }
            },
            "required": ["type", "description"],
            "additionalProperties": false
          }
        }
      },
      "required": ["id", "key", "label", "description", "type"],
      "additionalProperties": false
    }
  }
}
